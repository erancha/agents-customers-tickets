x-shared-db-env: &shared-db-env
  DB_URL: jdbc:mysql://mysql:3306/agentscustomerstickets_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
  DB_USERNAME: agentscustomerstickets
  DB_PASSWORD: agentscustomerstickets123

x-shared-integration-env: &shared-integration-env
  USERS_INTEGRATION_INTERNAL_API_KEY: ${USERS_INTEGRATION_INTERNAL_API_KEY:-internal-users-key}

x-shared-runtime-env: &shared-runtime-env
  SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-docker}

x-shared-cdc-env: &shared-cdc-env
  CDC_TOPIC_PARTITIONS: ${CDC_TOPIC_PARTITIONS:-2}

x-shared-restart-policy: &shared-restart-policy
  restart: on-failure:3
  deploy:
    restart_policy:
      condition: on-failure
      delay: 10s
      max_attempts: 3
      window: 30s

x-shared-app-env: &shared-app-env
  <<: [*shared-db-env, *shared-integration-env, *shared-runtime-env, *shared-cdc-env]
  USERS_INTEGRATION_MODE: ${USERS_INTEGRATION_MODE:-embedded}
  KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS:-redpanda:9092}
  WS_BROKER_RELAY_ENABLED: ${WS_BROKER_RELAY_ENABLED:-false}
  WS_BROKER_RELAY_HOST: ${WS_BROKER_RELAY_HOST:-rabbitmq}
  WS_BROKER_RELAY_PORT: ${WS_BROKER_RELAY_PORT:-61613}
  WS_BROKER_RELAY_CLIENT_LOGIN: ${WS_BROKER_RELAY_CLIENT_LOGIN:-guest}
  WS_BROKER_RELAY_CLIENT_PASSCODE: ${WS_BROKER_RELAY_CLIENT_PASSCODE:-guest}
  WS_BROKER_RELAY_SYSTEM_LOGIN: ${WS_BROKER_RELAY_SYSTEM_LOGIN:-guest}
  WS_BROKER_RELAY_SYSTEM_PASSCODE: ${WS_BROKER_RELAY_SYSTEM_PASSCODE:-guest}
  # LOGGING_LEVEL_ROOT: DEBUG
  LOGGING_PATTERN_CONSOLE: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5p [$${APP_INSTANCE:-unknown}] ${PID:- } --- [%t] %logger{39} : %m%n%wEx"

x-shared-app-base: &shared-app-base
  <<: *shared-restart-policy
  build:
    context: .
    dockerfile: Dockerfile
  image: agents-customers-tickets:local
  depends_on:
    mysql:
      condition: service_healthy
    debezium:
      condition: service_started
      required: false
  environment:
    <<: *shared-app-env

services:
  mysql:
    image: mysql:8.4
    command:
      - --server-id=1
      - --log-bin=mysql-bin
      - --binlog-format=ROW
      - --binlog-row-image=FULL
      - --binlog-expire-logs-seconds=86400
    environment:
      MYSQL_DATABASE: agentscustomerstickets_db
      MYSQL_USER: agentscustomerstickets
      MYSQL_PASSWORD: agentscustomerstickets123
      MYSQL_ROOT_PASSWORD: root
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-proot"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redpanda:
    profiles: ["cdc"]
    image: docker.redpanda.com/redpandadata/redpanda:v24.3.7
    command:
      - redpanda
      - start
      - --overprovisioned
      - --smp
      - "1"
      - --reserve-memory
      - 0M
      - --check=false
      - --node-id
      - "0"
      - --kafka-addr
      - internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr
      - internal://redpanda:9092,external://localhost:19092
      - --pandaproxy-addr
      - internal://0.0.0.0:8082,external://0.0.0.0:18082
      - --advertise-pandaproxy-addr
      - internal://redpanda:8082,external://localhost:18082
      - --rpc-addr
      - redpanda:33145
      - --advertise-rpc-addr
      - redpanda:33145
      - --set
      - redpanda.default_topic_partitions=${CDC_TOPIC_PARTITIONS:-2}
    ports:
      - "19092:19092"
      - "18082:18082"
      - "19644:9644"
    volumes:
      - redpanda_data:/var/lib/redpanda/data

  redpanda-console:
    profiles: ["cdc"]
    image: docker.redpanda.com/redpandadata/console:v2.8.4
    depends_on:
      - redpanda
    environment:
      KAFKA_BROKERS: redpanda:9092
      REDPANDA_ADMINAPI_ENABLED: "true"
      REDPANDA_ADMINAPI_URLS: http://redpanda:9644
    ports:
      - "8089:8080"

  debezium:
    profiles: ["cdc"]
    image: quay.io/debezium/server:3.1
    depends_on:
      mysql:
        condition: service_healthy
      redpanda:
        condition: service_started
    environment:
      <<: *shared-cdc-env
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS:-redpanda:9092}
    volumes:
      - ./config/debezium/application.properties:/debezium/config/application.properties:ro
      - debezium_data:/debezium/data

  app_lb_1:
    <<: *shared-app-base
    environment:
      <<: *shared-app-env
      APP_INSTANCE: lb_1

  app_lb_2:
    <<: *shared-app-base
    environment:
      <<: *shared-app-env
      APP_INSTANCE: lb_2

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      - app_lb_1
      - app_lb_2
    ports:
      - "${APP_PORT:-8080}:80"
    command:
      - /bin/sh
      - -lc
      - |
        cat > /etc/nginx/conf.d/default.conf <<'EOF'
        upstream app_upstream {
          server app_lb_1:8080;
          server app_lb_2:8080;
        }

        server {
          listen 80;

          location /ws/ {
            proxy_pass http://app_upstream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $$http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $$host;
            proxy_set_header X-Forwarded-For $$proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $$scheme;
            # WebSockets can be quiet for long periods; nginx defaults may close idle upstream connections (~60s).
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
            # Disable buffering for WS so frames are passed through immediately.
            proxy_buffering off;
          }

          location / {
            proxy_pass http://app_upstream;
            proxy_set_header Host $$host;
            proxy_set_header X-Forwarded-For $$proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $$scheme;
          }
        }
        EOF
        nginx -g 'daemon off;'

  rabbitmq:
    profiles: ["ws-relay"]
    image: rabbitmq:3.13.3-management
    command: >-
      /bin/sh -lc "rabbitmq-plugins enable --offline rabbitmq_stomp;
      rabbitmq-server"
    environment:
      RABBITMQ_DEFAULT_USER: ${WS_BROKER_RELAY_SYSTEM_LOGIN:-guest}
      RABBITMQ_DEFAULT_PASS: ${WS_BROKER_RELAY_SYSTEM_PASSCODE:-guest}
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 10s
      timeout: 5s
      retries: 20
    ports:
      - "61613:61613"
      - "15672:15672"

  users-service:
    profiles: ["users-ms"]
    <<: *shared-restart-policy
    build:
      context: .
      dockerfile: Dockerfile
    image: agents-customers-tickets:local
    container_name: agents-customers-tickets-users
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      <<: [*shared-db-env, *shared-integration-env, *shared-runtime-env]
      USERS_INTEGRATION_MODE: embedded
    ports:
      - "${USERS_SERVICE_PORT:-8081}:8080"

volumes:
  mysql_data: # Persist MySQL data between restarts.
  debezium_data: # Persist Debezium offsets between restarts.
  redpanda_data: # Persist Redpanda data between restarts.
