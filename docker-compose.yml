services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: supporthub_db
      MYSQL_USER: supporthub
      MYSQL_PASSWORD: supporthub123
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
