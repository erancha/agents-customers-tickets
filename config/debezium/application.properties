debezium.source.connector.class=io.debezium.connector.mysql.MySqlConnector

debezium.source.database.hostname=mysql
debezium.source.database.port=3306
debezium.source.database.user=root
debezium.source.database.password=root
debezium.source.database.server.id=5401
debezium.source.database.server.name=agentscustomerstickets
debezium.source.database.include.list=agentscustomerstickets_db

debezium.source.topic.prefix=my
debezium.source.include.schema.changes=false
debezium.source.column.exclude.list=agentscustomerstickets_db.users.password_hash

# Start from current binlog position for phase 1 (no initial table snapshot).
debezium.source.snapshot.mode=schema_only

debezium.sink.type=kafka
debezium.sink.kafka.producer.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS}
debezium.sink.kafka.producer.key.serializer=org.apache.kafka.common.serialization.StringSerializer
debezium.sink.kafka.producer.value.serializer=org.apache.kafka.common.serialization.StringSerializer

debezium.format.value=json
debezium.format.key=json

# Emit payload only (no Kafka Connect schema section).
debezium.source.key.converter.schemas.enable=false
debezium.source.value.converter.schemas.enable=false

debezium.source.schema.history.internal=io.debezium.storage.file.history.FileSchemaHistory
debezium.source.schema.history.internal.file.filename=/debezium/data/schemahistory.dat

debezium.source.offset.storage=org.apache.kafka.connect.storage.FileOffsetBackingStore
debezium.source.offset.storage.file.filename=/debezium/data/offsets.dat
debezium.source.offset.flush.interval.ms=1000